This certificate is the same one used by services during local development.

To prevent browser warnings, add the Centricient Root CA certificate as a trusted certificate in Keychain Access. 

For more information, check out the security repository at
http://gitlab.centricient.corp/development/security/tree/master

